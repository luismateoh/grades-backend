# Server properties
server:
  servlet:
    context-path: /grades-api
  port: 8080

# Spring properties
spring:
  # Dev is active by default. This value can be overwritten by java arg param
  profiles:
    active: heroku
  application:
    name: grades-api
  messages:
    basename: bundles/apiLanguageBundle